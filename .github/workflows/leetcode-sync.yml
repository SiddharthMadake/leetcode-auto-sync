name: LeetCode Sync

on:
  schedule:
    # ~10:37 PM, 11:07 PM, 11:37 PM, 11:58 PM, and 12:02 AM IST (odd minutes to dodge GitHub's peak-load delays)
    - cron: '7 17 * * *'
    - cron: '37 17 * * *'
    - cron: '7 18 * * *'
    - cron: '28 18 * * *'
    - cron: '32 18 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: leetcode-sync-main
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      LEETCODE_USERNAME: ${{ secrets.LEETCODE_USERNAME }}
      LEETCODE_SESSION: ${{ secrets.LEETCODE_SESSION }}
      LEETCODE_CSRF_TOKEN: ${{ secrets.LEETCODE_CSRF_TOKEN }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      DEFAULT_BRANCH: main
      SYNC_LOOKBACK_LIMIT: 100
      HTTP_TIMEOUT_SECONDS: 30
      HTTP_MAX_RETRIES: 4
      REPO_ROOT: .
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Run tests
        run: python -m pytest

      - name: Sync accepted LeetCode submissions
        run: python sync.py
